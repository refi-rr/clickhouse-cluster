# clickhouse-cluster
what is this ? this is docker compose setup for clickhouse cluster with ch-keeper and ch proxy (for balance)
#additional:
- deployed using coolify, more info go to official coolify
- you can replace zookeeper by clickhouse-keeper. You just replace the image and config the clickhouse server config.xml
- if you are using coolify, you can monitor the log in coolify dashboard
